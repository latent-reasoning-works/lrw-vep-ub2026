# Workshop notebook — pinned environment.
#
# Purpose: reproduce 01_workshop_followalong.ipynb exactly as the workshop ran.
# Tested with these floors against ESM-1b via HuggingFace transformers
# (no fair-esm; the notebook uses the HF route, not the fair-esm route).
#
# Local install (cleanest, ~2 minutes):
#   pip install -r requirements-workshop.txt
#   # OR with uv (preferred — uses pyproject.toml + uv.lock for bit-identical pins):
#   uv sync
#
# Then launch:
#   jupyter notebook 01_workshop_followalong.ipynb
#
# Colab install: the s1-install cell in the notebook installs the 8 core
# pins inline (jupyter / ipykernel are already on Colab). This file is the
# source of truth for the pip path; pyproject.toml mirrors it for the uv
# path; the s1-install cell mirrors the 8 core pins for Colab.
#
# These floors avoid:
#   - transformers <4.45 breakage on `output_hidden_states=True` for ESM
#   - numpy <2.0 missing `numpy.strings` (referenced by transformers 5.x);
#     we cap below 3 to stay ABI-stable
#   - scikit-learn <1.3 deprecations the notebook hits
#   - seaborn 0.12.x kdeplot API differences

# Core: notebook + harness logic.
transformers>=4.45,<6
torch>=2.0
numpy>=2.0,<3
scipy>=1.13          # first scipy with numpy-2.x C-ABI support
pandas>=2.0
scikit-learn>=1.3
seaborn>=0.13
tqdm>=4.65
matplotlib>=3.7

# Runtime: jupyter + ipykernel for `jupyter notebook` and `validate_notebook.py`
# (nbconvert + ExecutePreprocessor). Already preinstalled on Colab.
jupyter>=1.0
ipykernel>=6.0

# Slide-fallback script (`experiments/scripts/run_first_prompt.py`): UMAP of
# the embeddings + optional wandb logging. Phase 1's `00_demo_umap.py` uses
# the submodule's `[workshop]` extra for these; the standalone fallback runs
# from the notebook env, so the deps live here too.
umap-learn>=0.5
wandb>=0.16
