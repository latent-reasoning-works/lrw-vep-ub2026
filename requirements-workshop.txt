# Workshop notebook — pinned environment.
#
# Purpose: reproduce 01_workshop_followalong.ipynb exactly as the workshop ran.
# Encoder backend is fair-esm (Facebook's official ESM library — the one
# Brandes 2023 used). No transformers dependency chain, no tokenizers version
# pin, no numpy-ABI fight with Colab's pre-installed scientific stack.
#
# Local install:
#   uv sync                                       # preferred (from repo root)
#   pip install -r requirements-workshop.txt      # alternative
#
# Then launch:
#   uv run jupyter notebook experiments/notebooks/01_workshop_followalong.ipynb
#
# Colab install: the s1-install cell in the notebook installs the same pins
# inline (`pip install fair-esm` does most of the work).
#
# Floors avoid:
#   - numpy 2.x ABI issues — we don't actually need 2.x for fair-esm, but
#     keeping >=1.24 captures all modern numpy. Capped <3 for ABI stability.
#   - pandas 3.x breaks Colab's google-colab / bqplot / gradio
#   - scikit-learn <1.3 deprecations
#   - seaborn 0.12.x kdeplot API differences

# Core: notebook + harness logic.
fair-esm>=2.0
torch>=2.0
numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.3
seaborn>=0.13
tqdm>=4.65
matplotlib>=3.7

# Runtime: jupyter + ipykernel for `jupyter notebook` and `validate_notebook.py`
# (nbconvert + ExecutePreprocessor). Already preinstalled on Colab.
jupyter>=1.0
ipykernel>=6.0

# Slide-fallback script (`experiments/scripts/run_first_prompt.py`): UMAP of
# the embeddings + optional wandb logging. Phase 1's `00_demo_umap.py` uses
# the submodule's `[workshop]` extra for these; the standalone fallback runs
# from the notebook env, so the deps live here too.
umap-learn>=0.5
wandb>=0.16
